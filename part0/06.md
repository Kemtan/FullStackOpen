```mermaid
sequenceDiagram
    participant browser
    participant server

    browser->>server: POST https://studies.cs.helsinki.fi/exampleapp/new_note_spa
    activate server
    Note right of browser: User submit a new note (save button clicked), new note get pushed to notes variable
    Note right of browser: the content type in the header is JSON so the server know how to correctly parse the data
    server-->>browser: DOM with redrawNotes()
    deactivate server
    Note right of browser: update DOM with new note that user submitted